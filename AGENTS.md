# Weave 项目协作规则

## 开始工作

阅读 `context/current-state.md`、`docs/product-context.md`，再读取与任务相关的 PRD 段落、决策和代码。`context/manifest.json` 是入口索引。不要把整个历史对话当作默认上下文。

用户在本次会话明确提出的新要求优先。发现其与既有文件冲突时，记录变更并更新对应共识，不静默保留两个版本的产品方向。

## 产品边界

现有产品驱动持续售前运营，不增加每轮人工创建开发任务。必须覆盖客户背调、证据匹配、触达反馈、销售交接与产品机会。v0.2 背调包含回款资料与交易条件判断，但不得把销售认可或交接变成授信、保险或放货批准。共享知识必须保留来源、范围和确认状态。

公开信号、AI 推断、客户明确需求和企业确认能力不可混淆。产品数据变化必须使相关判断和草稿失效；不影响无关客户。资料缺失保持未知。

v0.3 新增售中订单确认与轻量客诉证据演示，详细范围见 `docs/order-confirmation-prd.md`。AI 解释与草稿、内部批准、客户接受、ERP 回执分别记录；相关变更使批准和草稿失效，保留历史与无关决定。报价决策、供应商整合、实际打样、采购、生产和履约不在本版执行范围。不要在企业用户界面放模型编排、FDE 或开发配置等无助于业务决策的技术内容。

## 实现约束

演示数据与实际连接清楚区分。当前版本没有真实发送、实时 AI、多人数据库或身份权限服务。不得将本地状态演示表述为已完成企业部署。

保持数据、业务状态和界面的职责。回款逻辑在 `src/diligence.mjs`，产品导入规则在 `src/ingestion.mjs`，扩展视图在 `src/business-views.mjs`。订单规则在 `src/orders.mjs`，订单视图在 `src/order-views.mjs`，老板故事源文件为 `src/boss.html`。新业务规则优先放在对应 engine 并增加必要的行为验证。不要直接修改生成的 `index.html` 或 `boss.html` 代替源码修改。

运行 `npm test` 与 `npm run build`。有界面改动时检查桌面、手机以及相关关键交互。说明实际验证结果和未覆盖内容。

不得提交客户真实邮件、联系人数据、凭证、内部价格表或模型密钥。使用脱敏 fixtures。对外发送、发布范围及生产权限须遵循用户实际授权。

## 协作交接

围绕具体 Issue 创建分支和 PR。PR 写清业务问题、改动、验收结果及待决事项。不要自动合并自己无法验证的变更。

`@0xrelander` 是项目协调人，负责分解议题、组织评审与整理阶段状态。产品范围、商业承诺和对外预算由 Nick 决定。角色文件与 CODEOWNERS 不替代实际成员邀请及仓库权限。

每次完成可交接工作后更新 `context/current-state.md`。改变长期产品或架构决定时追加 `docs/decisions/`；原决定标注被替代原因，不删除历史。原始调研材料放 `docs/sources.md` 的索引中，确认后的结论才进入产品共识。

本文件适用于不同模型和开发工具。任何单次聊天中提出的假设，未经确认不要写成既定事实。
